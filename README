this is just my personal setup. the only part  you might want is jg-functions/jg-keys, which set up my custom key commands


#Notes:

- `brew install global --with-pygments --with-ctags`
- pip install pygments


- install cask via ssh | bash (homebrew install doesn't work as of now): https://cask.readthedocs.io/en/latest/guide/installation.html
- cask install

- brew install fuzzy-find

$ defaults write org.gnu.Emacs AppleFontSmoothing -int 0
